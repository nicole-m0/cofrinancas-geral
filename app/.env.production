# Carregado automaticamente pelo Expo em modo produção
# (`expo export`, builds EAS, `expo start --no-dev`).
# API = Route Handlers do painel-admin publicados na Vercel.
EXPO_PUBLIC_API_URL=https://painel-admin-vert.vercel.app
